{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "PYTHON"
  },
  "deploy": {
    "startCommand": "flet web --directory . --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/",
    "healthcheckTimeout": 30
  }
}